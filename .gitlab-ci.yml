include:
  - project: 'barsic1/barsic-ci'
    file: '.back-end-template.yml'

variables:
  IMAGE_NAME: "tg-vpn-bot"
  REGISTRY_URL: "images.registry.twcstorage.ru"
  IMAGE_REPOSITORY: "tg"

binary-job:
  extends: .binary-job

build-job:
  extends: .build-job

upload-job:
  extends: .upload-job